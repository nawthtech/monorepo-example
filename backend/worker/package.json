{
  "name": "nawthtech-worker",
  "version": "1.0.0",
  "description": "NawthTech API Worker",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:prod": "wrangler deploy --env production"
  },
  "dependencies": {
    "itty-router": "^4.0.0",
    "mongodb": "^6.0.0"
  },
  "devDependencies": {
    "wrangler": "^4.51.0"
  }
}