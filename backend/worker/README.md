# NawthTech Cloudflare Worker

Cloudflare Worker with D1 Database for NawthTech backend API.

## 🚀 Quick Start

### Prerequisites
- Node.js 18+ and npm
- Cloudflare account
- Wrangler CLI: `npm install -g wrangler`

### Installation
```bash
cd worker
npm install